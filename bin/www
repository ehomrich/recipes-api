'use strict';

require('dotenv').config({ path: 'config/.env' });

const config = require('../config');

config.checkVariables();

var app = require('../app');

app.listen(3000);

module.exports = app;
